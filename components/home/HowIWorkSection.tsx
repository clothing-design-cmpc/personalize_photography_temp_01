// LensVerse — HowIWorkSection
import StorySection from "./StorySection";

export default function HowIWorkSection() {
  return (
    <StorySection
      eyebrow="How I Work"
      heading="People First, Camera Second"
      body="Every session starts with a real conversation, not a shot list. I want to know what makes you nervous, what you're proud of, what you want this moment to mean later. The best photos happen when people forget the camera is even there — and that only works if you feel safe with the person behind it."
      imagePosition="left"
    />
  );
}
